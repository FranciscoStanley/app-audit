{
  "name": "app-audit-monorepo",
  "version": "1.1.0",
  "private": true,
  "description": "App Audit — plataforma de auditoria de segurança GitHub",
  "author": "Francisco Stanley Rodrigues Albuquerque",
  "license": "MIT",
  "workspaces": [
    "BackEnd",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run start:dev -w BackEnd\" \"npm run dev -w frontend\"",
    "build": "npm run build -w BackEnd && npm run build -w frontend",
    "audit:miasma": "npm run audit:miasma -w BackEnd",
    "test": "npm run test -w BackEnd && npm run test -w frontend",
    "test:e2e": "npm run test:e2e -w BackEnd",
    "lint": "npm run lint -w BackEnd && npm run lint -w frontend",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d --build",
    "docker:stop": "docker compose stop",
    "docker:restart": "docker compose stop && docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:oracle": "docker compose -f docker-compose.oracle.yml up -d --build",
    "oracle:bootstrap": "bash scripts/oracle-cloud/01-bootstrap-vm.sh",
    "oracle:deploy": "bash scripts/oracle-cloud/02-deploy-app.sh",
    "oracle:health": "bash scripts/oracle-cloud/03-health-check.sh",
    "security:scan": "node scripts/check-secrets.mjs",
    "docs:screenshots": "node scripts/capture-screenshots.mjs",
    "users:update": "npm run users:update -w BackEnd",
    "setup:oauth": "node scripts/setup-github-oauth.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "playwright": "^1.52.0"
  }
}

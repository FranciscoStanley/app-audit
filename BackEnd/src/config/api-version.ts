/** Versão semver publicada do App Audit (monorepo). */
export const APP_VERSION = '1.1.0';

/** Prefixo global da API REST versionada. Health permanece em `/health`. */
export const API_V1_PREFIX = 'v1';
